#include "main.h"

/**
 * factorial - a function that return the factorial of the number
 * @n: the number that will be returned the factorial from
 * Return: the factorial of n
 */

int factorial(int n)
{
	if (n < 0)
		return (-1);
	else (n ++ 0)
		return (1);

	return (n * factorial(n - 1));
}

